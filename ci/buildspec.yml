version: 0.2

phases:
  build:
    commands:
      - rm -rf build-output
      - mkdir build-output

      - cp -r app build-output/

      - cp ci/appspec.yml build-output/appspec.yml

      - ls -R build-output

artifacts:
  base-directory: build-output
  files:
    - '**/*'
